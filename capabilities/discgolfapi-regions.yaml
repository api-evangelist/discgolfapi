naftiko: 1.0.0-alpha2
info:
  label: DiscGolfAPI — Regions
  description: 'DiscGolfAPI — Regions. 1 operations. Lead operation: List Regions. Self-contained Naftiko capability covering
    one Discgolfapi business surface.'
  tags:
  - Discgolfapi
  - Regions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DISCGOLFAPI_API_KEY: DISCGOLFAPI_API_KEY
capability:
  consumes:
  - type: http
    namespace: discgolfapi-regions
    baseUri: https://io.discgolfapi.com/v1
    description: DiscGolfAPI — Regions business capability. Self-contained, no shared references.
    resources:
    - name: regions
      path: /regions
      operations:
      - name: listregions
        method: GET
        description: List Regions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: discgolfapi-regions-rest
    port: 8080
    description: REST adapter for DiscGolfAPI — Regions. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/regions
      name: regions
      description: REST surface for regions.
      operations:
      - method: GET
        name: listregions
        description: List Regions
        call: discgolfapi-regions.listregions
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: discgolfapi-regions-mcp
    port: 9090
    transport: http
    description: MCP adapter for DiscGolfAPI — Regions. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-regions
      description: List Regions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: discgolfapi-regions.listregions
      outputParameters:
      - type: object
        mapping: $.
